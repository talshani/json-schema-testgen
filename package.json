{
  "name": "json-schema-typescript-testing",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "build-data": "bun src/build-test-data.ts",
    "generate": "bun src/cli.ts",
    "build": "bun build --compile src/cli.ts --outfile json-schema-testgen"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
